#include "main.h"

/**
 * main  -  function that checks for lowercase character.
 *
 * Return: 0
 */

int _islower(int c)
{
	
	if (_islower(c) != 0)
	{
	      return (1);
	}
	if else (_islower())
	{	
		return (0);	
	} else {

		return (0);
	}
}
