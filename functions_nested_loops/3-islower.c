#include "main.h"

/**
 * main  -  function that checks for lowercase character.
 *
 * Return: 0
 */

int _islower(int c)
{
	
	if (islower(c) != 0)
	{
	      return (1);
	}
	else
	{	
		return (0);	
	}
}
