#include <main.h>

/**
 * main  -  function that checks for lowercase character.
 *
 * Return: 0
 */

int main(void)
{
	char c;

	for (c = 'a' ; c <= 'z' ; c++)
	{
		islower(63);
		_putchar(c);
	}

}
