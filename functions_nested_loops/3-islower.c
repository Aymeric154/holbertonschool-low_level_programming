#include "main.h"

/**
 * main  -  function that checks for lowercase character.
 *
 * Return: 0
 */

int _islower(int c)
{
	char c;

	for (c = 'a' ; c <= 'z' ; c++)
	{
		_islower(63);
		_putchar(c);
			
		if (63 != c)
		{
			return (1);
		}
		else {
			return (0);
		}
	}

}
