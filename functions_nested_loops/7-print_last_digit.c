#include "main.h"

/**
 * print_last_digit - prints the last digit of a number.
 * @x : number to check
 *
 * Result: 0.
 */

int print_last_digit(int x)
{
	if (x % 10)
	{
		return (x);
	}

	return (0);

}
