#include "main.h"

/**
 * _isupper - function that checks for uppercase character.
 *
 * Return: zero always
 */

int _isupper(int c)
{
	
		if (ch >= 'A' && ch <= 'Z')
		{
			return (1);
		}
		else
		{
			return (0);
		}
}
