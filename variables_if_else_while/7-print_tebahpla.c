#include <stdio.h>

/**
 * main -  program that prints the lowercase alphabet in reverse
 *
 * Return:  alway zero
 */

int main(void)
{
	char apbt;

	for (apbt = 'z' ; apbt <= 'a'; apbt++)
	{

		putchar(apbt);

	}


	putchar('\n');

	return (0);
}
