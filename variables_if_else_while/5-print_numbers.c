#include <stdio.h>

/**
 * main - program that prints all single 
 * digit numbers of base 10 starting from 0
 *
 * Return: alway zero
 */

int main(void)
{
	int n;
	
	n = 0;

	if (n < 10 )
	{
		printf ("%d, ", n);
	}

	return (0);

}
